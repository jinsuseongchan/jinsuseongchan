spring:
  datasource:
    url: jdbc:postgresql://postgres-db.chdq0o2eoge6.ap-northeast-2.rds.amazonaws.com:5432/jinsuseongchan-dev
    username: dev
    password: devpostgres
    hikari:
      maximum-pool-size: 4

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    root: DEBUG