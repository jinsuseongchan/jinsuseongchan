spring:
  datasource:
    url: jdbc:postgresql://132.226.18.164:5432/test_db
    username: test
    password: test1234
    hikari:
      maximum-pool-size: 4

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    root: DEBUG