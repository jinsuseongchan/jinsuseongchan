name: Test
on:
  workflow_dispatch:

jobs:
  job1:
    uses: jinsuseongchan/.github/workflow-templates/octo-organization-ci.yaml

  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - run: echo "success"
